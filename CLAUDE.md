# Global Claude Code Instructions

## PR Creation Rules

ALWAYS use the `create-pr` skill when creating pull requests. Never create PRs manually without invoking this skill first. The `create-pr` skill will automatically use the `pr-description` skill to generate professional PR descriptions.
